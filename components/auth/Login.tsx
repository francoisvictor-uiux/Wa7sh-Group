"use client";

/**
 * Login — desktop-only for now.
 * Mobile and tablet versions will be added in a future phase.
 */

import { LoginDesktop } from "./LoginDesktop";

export function Login() {
  return <LoginDesktop />;
}
